# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Logs & runtime artifacts
*.log
__pycache__/

# OS / editor
.DS_Store
.AppleDouble
Thumbs.db
*.swp

# Secrets (never commit real credentials)
.env
*.key
secrets.json

# Large runtime artifacts / models (exclude from repo)
whisper/
data/

# PyInstaller build artifacts (the frozen desktop app is produced by
# desktop/build-*.py; keep the build scripts tracked, not the output)
build/
dist/
# Frozen desktop .app after it is moved into the project's apps/ folder
apps/
# Generated per-build version stamp (written by desktop/build-*.py)
app/version.json
